- If you compile "Drawer.java" you will see the first frame of the savanna map.

- All the classes work, but there is an issue with "status() and move()" methods, that 
  make the compiler jump once they get called.